This code should be copied into processing, which will interpret the data transmitted from the sensors and graph it 

In addition, it will log the data into a CSV file 

The Data folder has the fonts I used for my graph

I recommend downloading the fonts, so that the graph can run properly 